# Copiar a .env.development / .env.staging / .env.production según corresponda

MONGO_URI=

# Fintoc — conciliación (no commitear valores reales)
FINTOC_SECRET_KEY=
FINTOC_LINK_TOKEN=
FINTOC_ACCOUNT_ID=

# Importaciones: colecciones Mongo purchaseorders y shipments (misma DB que MONGO_URI)
